<div id="sticky-notes">
    <?php 
	
	foreach($cvdata as $cvmanagment):?>
		
		<?php foreach($cvmanagment as $cv):?>
	
    <div class="sticky-note">
        <textarea id="stickynote-<?php echo $cv->getId() ?>"><?php echo $cv->getEmployerName() ?></textarea>
        <a href="#" id="remove-<?php echo $cv->getId(); ?>"class="delete-sticky">X</a>
    </div>
	
		<hr>
		<?php endforeach; ?>
    <?php endforeach; ?>
    <div id="create">+</div>

</div>
<div class="clear-both"></div>